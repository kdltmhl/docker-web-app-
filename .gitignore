# .gitignore

# Log-Dateien
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Abhängigkeiten
# Dieser Ordner wird beim 'npm install' oder 'npm ci' Befehl automatisch erstellt
# und sollte niemals in die Versionskontrolle eingecheckt werden.
node_modules

# Build-Verzeichnisse
dist
build

# Umgebungsvariablen
# Diese Datei enthält sensible Daten wie API-Schlüssel oder Passwörter
# und darf niemals öffentlich auf GitHub landen.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Betriebssystem-spezifische Dateien
.DS_Store
Thumbs.db